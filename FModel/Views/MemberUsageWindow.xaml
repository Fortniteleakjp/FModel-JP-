<adonisControls:AdonisWindow x:Class="FModel.Views.MemberUsageWindow"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:converters="clr-namespace:FModel.Views.Resources.Converters"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                             WindowStartupLocation="CenterScreen" ResizeMode="CanResize" IconVisibility="Collapsed"
                             Height="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenHeight}, Converter={converters:RatioConverter}, ConverterParameter='0.65'}"
                             Width="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenWidth}, Converter={converters:RatioConverter}, ConverterParameter='0.7'}">
    <adonisControls:AdonisWindow.Style>
        <Style TargetType="adonisControls:AdonisWindow" BasedOn="{StaticResource {x:Type adonisControls:AdonisWindow}}">
            <Setter Property="Title" Value="{DynamicResource UI_MemberUsage_Title}" />
        </Style>
    </adonisControls:AdonisWindow.Style>

    <adonisControls:AdonisWindow.Resources>
        <!-- the themed DataGrid template realizes every row, a common function has thousands of uses (see TableViewerWindow) -->
        <Style x:Key="VirtualizedDataGrid" TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGrid}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="DG_ScrollViewer" Focusable="False" CanContentScroll="True"
                                          HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                                          VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}">
                                <ScrollViewer.Template>
                                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter" Grid.Column="1"
                                                                            Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />

                                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Grid.Row="1" Grid.ColumnSpan="2"
                                                                    CanContentScroll="{TemplateBinding CanContentScroll}" />

                                            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Row="1" Grid.Column="2" Orientation="Vertical"
                                                       Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}"
                                                       Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />

                                            <Grid Grid.Row="2" Grid.Column="1">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="{Binding NonFrozenColumnsViewportHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Column="1" Orientation="Horizontal"
                                                           Maximum="{TemplateBinding ScrollableWidth}" ViewportSize="{TemplateBinding ViewportWidth}"
                                                           Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                           Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                                            </Grid>
                                        </Grid>
                                    </ControlTemplate>
                                </ScrollViewer.Template>
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- the rows have their own background, which hides the theme's selection: the selected row is drawn in the accent color -->
        <Style x:Key="UsageCell" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="4 2" />
            <!-- the themed cell template doesn't draw its background -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter VerticalAlignment="Center" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource {x:Static adonisUi:Brushes.AccentBrush}}" />
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static adonisUi:Brushes.AccentForegroundBrush}}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="UsageRow" TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource {x:Static adonisUi:Brushes.Layer2HighlightBrush}}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </adonisControls:AdonisWindow.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- what to look for -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QueryBox" Grid.Column="0" Padding="4 3" KeyDown="OnQueryKeyDown"
                     adonisExtensions:WatermarkExtension.Watermark="{DynamicResource UI_MemberUsage_Query}" />
            <ComboBox x:Name="KindBox" Grid.Column="1" Margin="6 0 0 0" MinWidth="100" SelectedIndex="0">
                <ComboBoxItem Content="{DynamicResource UI_MemberUsage_KindAny}" />
                <ComboBoxItem Content="{DynamicResource UI_MemberUsage_KindFunction}" />
                <ComboBoxItem Content="{DynamicResource UI_MemberUsage_KindVariable}" />
            </ComboBox>
            <TextBox x:Name="ScopeBox" Grid.Column="2" Margin="6 0 0 0" Padding="4 3" KeyDown="OnQueryKeyDown"
                     adonisExtensions:WatermarkExtension.Watermark="{DynamicResource UI_MemberUsage_Scope}" />
            <Button x:Name="SearchButton" Grid.Column="3" Margin="6 0 0 0" Padding="14 3" MinWidth="80" Click="OnSearchClick"
                    Content="{DynamicResource UI_MemberUsage_Search}" />
        </Grid>

        <TextBlock Grid.Row="1" Margin="2 3 0 0" FontSize="11" Opacity=".6" TextWrapping="Wrap"
                   Text="{DynamicResource UI_MemberUsage_QueryHint}" />

        <!-- members of the blueprint the window was opened from -->
        <DockPanel x:Name="MemberPanel" Grid.Row="2" Margin="0 6 0 0" Visibility="Collapsed">
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" Margin="2 0 8 0" Text="{DynamicResource UI_MemberUsage_Members}" />
            <ComboBox x:Name="MemberPicker" SelectionChanged="OnMemberPicked" DisplayMemberPath="Display"
                      IsTextSearchEnabled="True" TextSearch.TextPath="Name" MaxDropDownHeight="480" />
        </DockPanel>

        <!-- results toolbar -->
        <DockPanel Grid.Row="3" Margin="0 8 0 6">
            <Button x:Name="OpenGraphButton" DockPanel.Dock="Right" Padding="10 3" Margin="6 0 0 0" Click="OnOpenGraphClick"
                    Content="{DynamicResource UI_MemberUsage_OpenGraph}" IsEnabled="False" />
            <Button x:Name="OpenButton" DockPanel.Dock="Right" Padding="10 3" Margin="6 0 0 0" Click="OnOpenClick"
                    Content="{DynamicResource UI_MemberUsage_Open}" IsEnabled="False" />
            <TextBox x:Name="FilterBox" Padding="4 2" TextChanged="OnFilterTextChanged"
                     adonisExtensions:WatermarkExtension.Watermark="{DynamicResource UI_MemberUsage_Filter}" />
        </DockPanel>

        <Grid Grid.Row="4">
            <DataGrid x:Name="UsageGrid" Style="{StaticResource VirtualizedDataGrid}" AutoGenerateColumns="False" IsReadOnly="True"
                      CellStyle="{StaticResource UsageCell}" RowStyle="{StaticResource UsageRow}"
                      SelectionMode="Extended" CanUserAddRows="False" CanUserDeleteRows="False" CanUserSortColumns="True"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      EnableRowVirtualization="True" EnableColumnVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                      MouseDoubleClick="OnUsageDoubleClick" SelectionChanged="OnSelectionChanged"
                      Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"
                      RowBackground="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"
                      Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                      HorizontalGridLinesBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="{DynamicResource UI_MemberUsage_OpenGraph}" Click="OnOpenGraphClick" />
                        <MenuItem Header="{DynamicResource UI_MemberUsage_Open}" Click="OnOpenClick" />
                        <Separator />
                        <MenuItem Header="{DynamicResource UI_MemberUsage_CopyPath}" Click="OnCopyPathClick" />
                        <MenuItem Header="{DynamicResource UI_MemberUsage_CopyRows}" Click="OnCopyRowsClick" />
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource UI_MemberUsage_ColKind}" Binding="{Binding KindText}" Width="80" />
                    <DataGridTextColumn Header="{DynamicResource UI_MemberUsage_ColAsset}" Binding="{Binding Name}" Width="230" />
                    <DataGridTextColumn Header="{DynamicResource UI_MemberUsage_ColUsedIn}" Binding="{Binding UsedIn}" Width="230" />
                    <DataGridTextColumn Header="{DynamicResource UI_MemberUsage_ColMember}" Binding="{Binding Member}" Width="230" />
                    <DataGridTextColumn Header="{DynamicResource UI_MemberUsage_ColCount}" Binding="{Binding Count}" Width="55" />
                    <DataGridTextColumn Header="{DynamicResource UI_MemberUsage_ColPath}" Binding="{Binding Path}" Width="*" MinWidth="200" />
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock x:Name="EmptyText" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Opacity=".7" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="520" />
        </Grid>

        <StackPanel Grid.Row="5" Margin="2 6 2 0">
            <ProgressBar x:Name="Progress" Height="4" Margin="0 0 0 4" Visibility="Collapsed" />
            <ItemsControl x:Name="NoteList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0 0 0 2"
                                   Foreground="{DynamicResource {x:Static adonisUi:Brushes.AlertBrush}}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <DockPanel>
                <Button x:Name="ClearCacheButton" DockPanel.Dock="Right" Padding="8 1" Margin="8 0 0 0" FontSize="11" Click="OnClearCacheClick"
                        Content="{DynamicResource UI_MemberUsage_ClearCache}" ToolTip="{DynamicResource UI_MemberUsage_ClearCacheTip}" />
                <TextBlock x:Name="CacheText" DockPanel.Dock="Right" VerticalAlignment="Center" FontSize="11" Opacity=".6" TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="Status" VerticalAlignment="Center" Opacity=".7" TextTrimming="CharacterEllipsis" />
            </DockPanel>
        </StackPanel>
    </Grid>
</adonisControls:AdonisWindow>

<adonisControls:AdonisWindow x:Class="FModel.Views.WorldOutlinerWindow"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                             mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="720"

                             xmlns:viewModels="clr-namespace:FModel.ViewModels"
                             xmlns:controls="clr-namespace:FModel.Views.Resources.Controls"
                             xmlns:converters="clr-namespace:FModel.Views.Resources.Converters"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"

                             WindowStartupLocation="CenterScreen" ResizeMode="CanResize" IconVisibility="Collapsed"
                             Closing="OnClosing"
                             Height="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenHeight}, Converter={converters:RatioConverter}, ConverterParameter='0.75'}"
                             Width="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenWidth}, Converter={converters:RatioConverter}, ConverterParameter='0.78'}">
    <adonisControls:AdonisWindow.Style>
        <Style TargetType="adonisControls:AdonisWindow" BasedOn="{StaticResource {x:Type adonisControls:AdonisWindow}}">
            <Setter Property="Title" Value="{DynamicResource UI_WorldOutliner}" />
        </Style>
    </adonisControls:AdonisWindow.Style>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0 0 0 6">
            <Button x:Name="OpenJsonButton" DockPanel.Dock="Right" Padding="10 3" Margin="8 0 0 0" Click="OnOpenJsonClick"
                    Content="{DynamicResource UI_WorldOutliner_OpenJson}" IsEnabled="False" />
            <TextBox x:Name="FilterBox" Padding="4 2" TextChanged="OnFilterTextChanged"
                     adonisExtensions:WatermarkExtension.Watermark="{DynamicResource UI_WorldOutliner_Filter}" />
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="260" />
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="420" MinWidth="240" />
            </Grid.ColumnDefinitions>

            <TreeView x:Name="ActorTree" Grid.Column="0" BorderThickness="1" SelectedItemChanged="OnActorSelected"
                      BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}"
                      Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"
                      VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <TreeView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </TreeView.ItemsPanel>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem" BasedOn="{StaticResource TreeViewItemStyle}">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Setter Property="controls:TreeViewItemBehavior.IsBroughtIntoViewWhenSelected" Value="True" />
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type viewModels:WorldActorNode}" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal" Margin="0 1">
                            <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFolder}" Value="True">
                                                <Setter Property="FontWeight" Value="SemiBold" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Text="{Binding Hint}" Margin="8 0 0 0" Opacity=".45" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" ResizeDirection="Columns"
                          Background="{DynamicResource {x:Static adonisUi:Brushes.Layer3BackgroundBrush}}" />

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="ActorTitle" Grid.Row="0" Margin="2 0 2 2" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="ActorTransform" Grid.Row="1" Margin="2 0 2 6" Opacity=".7" TextWrapping="Wrap" />

                <DataGrid x:Name="PropertyGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                          CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          EnableRowVirtualization="True"
                          Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"
                          RowBackground="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"
                          Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                          HorizontalGridLinesBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource UI_WorldOutliner_Property}" Binding="{Binding Key}" Width="160" />
                        <DataGridTextColumn Header="{DynamicResource UI_WorldOutliner_Value}" Binding="{Binding Value}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <Expander x:Name="StreamingExpander" Grid.Row="3" Margin="0 6 0 0" Visibility="Collapsed"
                          Header="{DynamicResource UI_WorldOutliner_StreamingLevels}">
                    <ListBox x:Name="StreamingList" MaxHeight="160" BorderThickness="1"
                             BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}"
                             Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis" ToolTip="{Binding}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Expander>
            </Grid>
        </Grid>

        <TextBlock x:Name="Status" Grid.Row="2" Margin="2 6 2 0" Opacity=".7" TextTrimming="CharacterEllipsis" />
    </Grid>
</adonisControls:AdonisWindow>

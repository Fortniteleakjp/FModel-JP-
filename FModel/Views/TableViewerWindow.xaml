<adonisControls:AdonisWindow x:Class="FModel.Views.TableViewerWindow"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                             mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="720"

                             xmlns:converters="clr-namespace:FModel.Views.Resources.Converters"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"

                             WindowStartupLocation="CenterScreen" ResizeMode="CanResize" IconVisibility="Collapsed"
                             Height="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenHeight}, Converter={converters:RatioConverter}, ConverterParameter='0.7'}"
                             Width="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenWidth}, Converter={converters:RatioConverter}, ConverterParameter='0.8'}">
    <adonisControls:AdonisWindow.Style>
        <Style TargetType="adonisControls:AdonisWindow" BasedOn="{StaticResource {x:Type adonisControls:AdonisWindow}}">
            <Setter Property="Title" Value="{DynamicResource UI_TableViewer}" />
        </Style>
    </adonisControls:AdonisWindow.Style>

    <adonisControls:AdonisWindow.Resources>
        <!--
            The themed DataGrid template realizes every row instead of virtualizing, which makes a
            table of a hundred thousand rows impossible to open. This restores the standard
            DataGrid template (scroll viewer + ScrollContentPresenter) while keeping the theme colors.
        -->
        <Style x:Key="VirtualizedDataGrid" TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGrid}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="DG_ScrollViewer" Focusable="False" CanContentScroll="True"
                                          HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                                          VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}">
                                <ScrollViewer.Template>
                                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter" Grid.Column="1"
                                                                            Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />

                                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Grid.Row="1" Grid.ColumnSpan="2"
                                                                    CanContentScroll="{TemplateBinding CanContentScroll}" />

                                            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Row="1" Grid.Column="2" Orientation="Vertical"
                                                       Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}"
                                                       Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />

                                            <Grid Grid.Row="2" Grid.Column="1">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="{Binding NonFrozenColumnsViewportHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Column="1" Orientation="Horizontal"
                                                           Maximum="{TemplateBinding ScrollableWidth}" ViewportSize="{TemplateBinding ViewportWidth}"
                                                           Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                           Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                                            </Grid>
                                        </Grid>
                                    </ControlTemplate>
                                </ScrollViewer.Template>
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </adonisControls:AdonisWindow.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="120" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--toolbar-->
        <DockPanel Grid.Row="0" Margin="0 0 0 6" LastChildFill="True">
            <ComboBox x:Name="TableSelector" DockPanel.Dock="Left" MinWidth="220" Margin="0 0 8 0"
                      SelectionChanged="OnTableSelectionChanged" />
            <Button x:Name="ExportCsvButton" DockPanel.Dock="Right" Padding="10 3" Click="OnExportCsvClick"
                    Content="{DynamicResource UI_TableViewer_ExportCsv}" />
            <TextBox x:Name="FilterBox" Margin="0 0 8 0" Padding="4 2" TextChanged="OnFilterTextChanged"
                     adonisExtensions:WatermarkExtension.Watermark="{DynamicResource UI_TableViewer_Filter}" />
        </DockPanel>

        <DataGrid x:Name="Grid" Grid.Row="1" Style="{StaticResource VirtualizedDataGrid}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False" CanUserSortColumns="True"
                  SelectionMode="Extended" SelectionUnit="CellOrRowHeader" HeadersVisibility="Column"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  GridLinesVisibility="All" SelectionChanged="OnRowSelectionChanged" Sorting="OnSorting"
                  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                  Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"
                  RowBackground="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"
                  AlternatingRowBackground="{DynamicResource {x:Static adonisUi:Brushes.Layer3BackgroundBrush}}"
                  Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                  HorizontalGridLinesBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}"
                  VerticalGridLinesBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{DynamicResource UI_TableViewer_CopyCell}" Click="OnCopyCellClick" />
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <!--curve tables get a plot of the selected row-->
        <GridSplitter x:Name="CurveSplitter" Grid.Row="2" Height="4" HorizontalAlignment="Stretch" Visibility="Collapsed"
                      Background="{DynamicResource {x:Static adonisUi:Brushes.Layer3BackgroundBrush}}" />
        <Border x:Name="CurvePanel" Grid.Row="3" Height="200" Margin="0 4 0 0" Visibility="Collapsed"
                BorderThickness="1" BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}"
                Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}">
            <Grid>
                <Canvas x:Name="CurveCanvas" ClipToBounds="True" SizeChanged="OnCurveCanvasSizeChanged" />
                <TextBlock x:Name="CurveCaption" Margin="8 6" VerticalAlignment="Top" HorizontalAlignment="Left"
                           Opacity=".7" IsHitTestVisible="False" />
            </Grid>
        </Border>

        <TextBlock x:Name="Status" Grid.Row="4" Margin="2 6 2 0" Opacity=".7" TextTrimming="CharacterEllipsis" />
    </Grid>
</adonisControls:AdonisWindow>

<adonisControls:AdonisWindow x:Class="FModel.Views.AthenaQueueManagerWindow"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                             WindowStartupLocation="CenterScreen"
                             ResizeMode="CanResize"
                             IconVisibility="Collapsed"
                             Width="760"
                             Height="480"
                             MinWidth="560"
                             MinHeight="340">
    <adonisControls:AdonisWindow.Style>
        <Style TargetType="adonisControls:AdonisWindow"
               BasedOn="{StaticResource {x:Type adonisControls:AdonisWindow}}">
            <Setter Property="Title" Value="{DynamicResource UI_ManageAthenaQueue}" />
        </Style>
    </adonisControls:AdonisWindow.Style>

    <Grid Margin="10" adonisExtensions:LayerExtension.Layer="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="SemiBold">
            <Run Text="{DynamicResource UI_AthenaQueueCount}" />
            <Run Text="{Binding Application.AthenaQueueCount, Mode=OneWay}" />
        </TextBlock>

        <ListView Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  SelectionMode="Single"
                  BorderThickness="1"
                  BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}"
                  Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{DynamicResource UI_Name}"
                                    Width="220"
                                    DisplayMemberBinding="{Binding NameWithoutExtension}" />
                    <GridViewColumn Header="{DynamicResource UI_Path}"
                                    Width="500"
                                    DisplayMemberBinding="{Binding Path}" />
                </GridView>
            </ListView.View>
        </ListView>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Padding="10 3"
                    Content="{DynamicResource UI_RemoveFromQueue}"
                    IsEnabled="{Binding HasSelection}"
                    Click="OnRemoveSelectedClick" />
            <Button Grid.Column="2"
                    Padding="10 3"
                    Content="{DynamicResource UI_ClearAthenaQueue}"
                    IsEnabled="{Binding HasItems}"
                    Click="OnClearClick" />
            <Button Grid.Column="4"
                    Padding="10 3"
                    Style="{DynamicResource {x:Static adonisUi:Styles.AccentButton}}"
                    Content="{DynamicResource UI_CreateAthenaProfileFromQueue}"
                    IsEnabled="{Binding HasItems}"
                    Click="OnCreateProfileClick" />
            <Button Grid.Column="6"
                    Padding="10 3"
                    Content="{DynamicResource UI_Close}"
                    Click="OnCloseClick" />
        </Grid>
    </Grid>
</adonisControls:AdonisWindow>

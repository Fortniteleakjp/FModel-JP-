<UserControl x:Class="FModel.Views.VideoPlayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Focusable="True"
             MouseDown="OnMouseDown"
             PreviewKeyDown="OnPreviewKeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <MediaElement x:Name="MediaPlayer" 
                      LoadedBehavior="Manual" 
                      UnloadedBehavior="Stop" 
                      Stretch="Uniform"
                      MediaOpened="MediaPlayer_MediaOpened"
                      MediaEnded="MediaPlayer_MediaEnded"/>
                      
        <Grid Grid.Row="1" Background="#202020">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" Margin="10">
                <Button Content="再生" Click="Play_Click" Margin="0,0,5,0" Padding="10,5"/>
                <Button Content="一時停止" Click="Pause_Click" Margin="0,0,5,0" Padding="10,5"/>
                <Button Content="停止" Click="Stop_Click" Padding="10,5"/>
            </StackPanel>
            
            <Slider x:Name="TimeSlider" Grid.Column="1" VerticalAlignment="Center" Margin="10,0" 
                    Thumb.DragStarted="TimeSlider_ThumbDragStarted" 
                    Thumb.DragCompleted="TimeSlider_ThumbDragCompleted"
                    ValueChanged="TimeSlider_ValueChanged"/>
            
            <TextBlock x:Name="TimeText" Grid.Column="2" Text="00:00 / 00:00" VerticalAlignment="Center" Margin="10" Foreground="White"/>
            
            <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="0,0,10,0" VerticalAlignment="Center">
                <TextBlock Text="Vol" Foreground="White" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <Slider x:Name="VolumeSlider" Width="80" Minimum="0" Maximum="1" Value="0.5" ValueChanged="VolumeSlider_ValueChanged" VerticalAlignment="Center"/>
            </StackPanel>
            
            <ComboBox x:Name="SpeedComboBox" Grid.Column="4" Width="60" Margin="0,0,10,0" VerticalAlignment="Center" SelectionChanged="SpeedComboBox_SelectionChanged">
                <ComboBoxItem Content="0.5x" Tag="0.5"/>
                <ComboBoxItem Content="1.0x" Tag="1.0" IsSelected="True"/>
                <ComboBoxItem Content="1.5x" Tag="1.5"/>
                <ComboBoxItem Content="2.0x" Tag="2.0"/>
            </ComboBox>
        </Grid>
    </Grid>
</UserControl>
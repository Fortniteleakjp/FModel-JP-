<adonisControls:AdonisWindow x:Class="FModel.Views.LoadingInfoWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:vm="clr-namespace:FModel.ViewModels"
             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
             mc:Ignorable="d"
             Title="Loading Fortnite [LIVE]" Height="200" Width="400"
             WindowStartupLocation="CenterScreen"
             d:DataContext="{d:DesignInstance vm:LoadingInfoWindowViewModel}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding StatusText}" Grid.Row="0" FontWeight="Bold" />

        <ProgressBar Value="{Binding ProgressValue}" IsIndeterminate="{Binding IsIndeterminate}" Grid.Row="1" Margin="0,5,0,0" Height="20"/>
        <TextBlock Text="{Binding ProgressText}" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="経過時間:" FontWeight="Bold"/>
                <TextBlock Text="{Binding ElapsedTime}"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="残り時間:" FontWeight="Bold"/>
                <TextBlock Text="{Binding EstimatedTime}"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock Text="ダウンロードサイズ:" FontWeight="Bold"/>
            <TextBlock Text="{Binding DownloadSize}"/>
        </StackPanel>
    </Grid>
</adonisControls:AdonisWindow>

<Window x:Class="FModel.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:FModel.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:ProgressWindowViewModel}"
        Title="Progress" Height="150" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Message}" Grid.Row="0" Margin="0,0,0,5"/>

        <ProgressBar Value="{Binding Progress}" Minimum="0" Maximum="100" Grid.Row="1"/>
        <TextBlock Text="{Binding ETA}" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Button Content="Cancel" Command="{Binding CancelCommand}" Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0" Padding="10,5"/>
    </Grid>
</Window>
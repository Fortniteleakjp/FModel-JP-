<adonisControls:AdonisWindow x:Class="FModel.Views.BackupManager"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:converters="clr-namespace:FModel.Views.Resources.Converters"
                             xmlns:settings="clr-namespace:FModel.Settings"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                             WindowStartupLocation="CenterScreen"
                             ResizeMode="CanResize"
                             IconVisibility="Collapsed"
                             SizeToContent="Height"
                             Loaded="OnLoaded"
                             Width="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenWidth}, Converter={converters:RatioConverter}, ConverterParameter='0.225'}">
    <adonisControls:AdonisWindow.Style>
        <Style TargetType="adonisControls:AdonisWindow"
               BasedOn="{StaticResource {x:Type adonisControls:AdonisWindow}}">
            <Setter Property="Title"
                    Value="{DynamicResource Backup_Title}" />
        </Style>
    </adonisControls:AdonisWindow.Style>
    <adonisControls:AdonisWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <LinearGradientBrush x:Key="FModelJP5WindowBackgroundDark" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#111827" Offset="0" />
                <GradientStop Color="#1F2937" Offset="0.5" />
                <GradientStop Color="#0B1220" Offset="1" />
            </LinearGradientBrush>
            <LinearGradientBrush x:Key="FModelJP5WindowBackgroundLight" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F4F7FB" Offset="0" />
                <GradientStop Color="#EAF0F8" Offset="0.5" />
                <GradientStop Color="#DEE7F2" Offset="1" />
            </LinearGradientBrush>
            <SolidColorBrush x:Key="FModelJP5InputBackgroundDark"  Color="#CC0F172A" />
            <SolidColorBrush x:Key="FModelJP5InputBackgroundLight" Color="#FFFDFEFF" />
            <SolidColorBrush x:Key="FModelJP5InputBorderDark"      Color="#4D93C5FD" />
            <SolidColorBrush x:Key="FModelJP5InputBorderLight"     Color="#334980B8" />

            <Style x:Key="FModelJP5Card" TargetType="Border">
                <Setter Property="CornerRadius"    Value="14" />
                <Setter Property="Padding"         Value="16" />
                <Setter Property="Background"      Value="#2B111827" />
                <Setter Property="BorderBrush"     Value="#33000000" />
                <Setter Property="BorderThickness" Value="1" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UseDarkTheme, Source={x:Static settings:UserSettings.Default}}" Value="False">
                        <Setter Property="Background"  Value="#EFFFFFFF" />
                        <Setter Property="BorderBrush" Value="#22111C2E" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="FModelJP5ComboBox" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                <Setter Property="adonisExtensions:CornerRadiusExtension.CornerRadius" Value="10" />
                <Setter Property="Background"      Value="{StaticResource FModelJP5InputBackgroundDark}" />
                <Setter Property="BorderBrush"     Value="{StaticResource FModelJP5InputBorderDark}" />
                <Setter Property="BorderThickness" Value="1" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UseDarkTheme, Source={x:Static settings:UserSettings.Default}}" Value="False">
                        <Setter Property="Background"  Value="{StaticResource FModelJP5InputBackgroundLight}" />
                        <Setter Property="BorderBrush" Value="{StaticResource FModelJP5InputBorderLight}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="FModelJP5PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Cursor"          Value="Hand" />
                <Setter Property="Padding"         Value="10,7" />
                <Setter Property="FontWeight"      Value="SemiBold" />
                <Setter Property="Background"      Value="{DynamicResource {x:Static adonisUi:Brushes.AccentBrush}}" />
                <Setter Property="BorderBrush"     Value="#664A90E2" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="adonisExtensions:CornerRadiusExtension.CornerRadius" Value="12" />
            </Style>

            <Style x:Key="FModelJP5OrangeButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Cursor"          Value="Hand" />
                <Setter Property="Padding"         Value="10,7" />
                <Setter Property="FontWeight"      Value="SemiBold" />
                <Setter Property="Background"      Value="#FFF9A000" />
                <Setter Property="BorderBrush"     Value="#99E08800" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="adonisExtensions:CornerRadiusExtension.CornerRadius" Value="12" />
            </Style>

            <Style x:Key="FModelJP5SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Cursor"          Value="Hand" />
                <Setter Property="Padding"         Value="10,7" />
                <Setter Property="FontWeight"      Value="SemiBold" />
                <Setter Property="Background"      Value="#99213357" />
                <Setter Property="BorderBrush"     Value="#664A90E2" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="adonisExtensions:CornerRadiusExtension.CornerRadius" Value="12" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UseDarkTheme, Source={x:Static settings:UserSettings.Default}}" Value="False">
                        <Setter Property="Background"  Value="#FFEAF2FF" />
                        <Setter Property="BorderBrush" Value="#335C8DC4" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </adonisControls:AdonisWindow.Resources>

    <Grid adonisExtensions:LayerExtension.Layer="2">
        <Border Padding="14">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="{StaticResource FModelJP5WindowBackgroundDark}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding UseDarkTheme, Source={x:Static settings:UserSettings.Default}}" Value="False">
                            <Setter Property="Background" Value="{StaticResource FModelJP5WindowBackgroundLight}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel>

                <!-- 説明カード -->
                <Border Style="{StaticResource FModelJP5Card}" Margin="0,0,0,10">
                    <TextBlock TextAlignment="Center"
                               TextWrapping="Wrap"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="12">
                        <Run Text="{DynamicResource Backup_Desc_Intro}" />
                        <LineBreak /><LineBreak />
                        <Run FontWeight="Bold" Text="{DynamicResource Backup_Desc_Small_Title}" />
                        <Run Text="{DynamicResource Backup_Desc_Small_Detail}" />
                        <LineBreak /><LineBreak />
                        <Run FontWeight="Bold" Text="{DynamicResource Backup_Desc_Large_Title}" />
                        <Run Text="{DynamicResource Backup_Desc_Large_Detail}" />
                    </TextBlock>
                </Border>

                <!-- バックアップ作成カード -->
                <Border Style="{StaticResource FModelJP5Card}" Margin="0,0,0,10">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Click="OnCreateBackupClick"
                                    Style="{StaticResource FModelJP5PrimaryButton}"
                                    Content="{DynamicResource Backup_Btn_CreateSmall}"
                                    IsEnabled="{Binding IsCreatingBackup, Converter={x:Static converters:InvertBooleanConverter.Instance}}" />
                            <Button Grid.Column="2"
                                    Click="OnCreateBackupHeavyClick"
                                    Style="{StaticResource FModelJP5OrangeButton}"
                                    Content="{DynamicResource Backup_Btn_CreateLarge}"
                                    IsEnabled="{Binding IsCreatingBackup, Converter={x:Static converters:InvertBooleanConverter.Instance}}" />
                        </Grid>
                        <Button Style="{StaticResource FModelJP5SecondaryButton}"
                                Content="pakの比較ファイルを保存 (.loli)"
                                Command="{Binding CreateLoliBackupCommand}"
                                Click="Button_Click"
                                IsEnabled="{Binding IsCreatingBackup, Converter={x:Static converters:InvertBooleanConverter.Instance}}" />
                    </StackPanel>
                </Border>

                <!-- クラウド小バックアップカード -->
                <Border Style="{StaticResource FModelJP5Card}" Margin="0,0,0,10">
                    <StackPanel>
                        <Separator Style="{StaticResource CustomSeparator}"
                                   Tag="{DynamicResource Backup_Sep_CloudSmall}"
                                   Margin="0,0,0,8" />
                        <ComboBox Margin="0,0,0,8"
                                  ItemsSource="{Binding BackupsView}"
                                  SelectedItem="{Binding SelectedBackup, Mode=TwoWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FileName}" TextTrimming="CharacterEllipsis" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                            <ComboBox.Style>
                                <Style TargetType="ComboBox" BasedOn="{StaticResource FModelJP5ComboBox}">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BackupsView.Count}" Value="0">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                        </ComboBox>
                        <Button Content="{DynamicResource Backup_Btn_Download}"
                                Click="OnDownloadClick">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource FModelJP5SecondaryButton}">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BackupsView.Count}" Value="0">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- クラウド大バックアップカード -->
                <Border Style="{StaticResource FModelJP5Card}">
                    <StackPanel>
                        <Separator Style="{StaticResource CustomSeparator}"
                                   Tag="{DynamicResource Backup_Sep_CloudLarge}"
                                   Margin="0,0,0,8" />
                        <ComboBox Margin="0,0,0,8"
                                  ItemsSource="{Binding BigBackupsView}"
                                  SelectedItem="{Binding SelectedBigBackup, Mode=TwoWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Key}" TextTrimming="CharacterEllipsis" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                            <ComboBox.Style>
                                <Style TargetType="ComboBox" BasedOn="{StaticResource FModelJP5ComboBox}">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BigBackupsView.Count}" Value="0">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                        </ComboBox>
                        <Button Content="{DynamicResource Backup_Btn_DownloadLarge}"
                                Click="OnDownloadBigBackupClick">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource FModelJP5SecondaryButton}">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BigBackupsView.Count}" Value="0">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>

            </StackPanel>
        </Border>
    </Grid>
</adonisControls:AdonisWindow>

<adonisControls:AdonisWindow x:Class="FModel.Views.UpdateView"

                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:local="clr-namespace:FModel"
                             xmlns:settings="clr-namespace:FModel.Settings"
                             xmlns:converters="clr-namespace:FModel.Views.Resources.Converters"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             xmlns:controls="clr-namespace:FModel.Views.Resources.Controls"
                             WindowStartupLocation="CenterScreen" IconVisibility="Collapsed" ResizeMode="CanMinimize" Loaded="OnLoaded"
                             MinHeight="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenHeight}, Converter={converters:RatioConverter}, ConverterParameter='0.40'}"
                             Width="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenWidth}, Converter={converters:RatioConverter}, ConverterParameter='0.35'}">
    <adonisControls:AdonisWindow.Style>
        <Style TargetType="adonisControls:AdonisWindow" BasedOn="{StaticResource {x:Type adonisControls:AdonisWindow}}" >
            <Setter Property="Title" Value="{DynamicResource Update_Title}" />
        </Style>
    </adonisControls:AdonisWindow.Style>
    <adonisControls:AdonisWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <LinearGradientBrush x:Key="UpdateJP5WindowBackgroundDark" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#111827" Offset="0" />
                <GradientStop Color="#1F2937" Offset="0.55" />
                <GradientStop Color="#0B1220" Offset="1" />
            </LinearGradientBrush>
            <LinearGradientBrush x:Key="UpdateJP5WindowBackgroundLight" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F4F7FB" Offset="0" />
                <GradientStop Color="#EAF0F8" Offset="0.55" />
                <GradientStop Color="#DEE7F2" Offset="1" />
            </LinearGradientBrush>
            <Style x:Key="UpdateJP5Card" TargetType="Border">
                <Setter Property="CornerRadius" Value="14" />
                <Setter Property="Padding" Value="12" />
                <Setter Property="Background" Value="#2B111827" />
                <Setter Property="BorderBrush" Value="#33000000" />
                <Setter Property="BorderThickness" Value="1" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UseDarkTheme, Source={x:Static settings:UserSettings.Default}}" Value="False">
                        <Setter Property="Background" Value="#EFFFFFFF" />
                        <Setter Property="BorderBrush" Value="#22111C2E" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="UpdateJP5PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Padding" Value="14,8" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="MinHeight" Value="34" />
                <Setter Property="Background" Value="{DynamicResource {x:Static adonisUi:Brushes.AccentBrush}}" />
                <Setter Property="BorderBrush" Value="#664A90E2" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="adonisExtensions:CornerRadiusExtension.CornerRadius" Value="10" />
            </Style>
            <Style x:Key="UpdateJP5SecondaryButton" TargetType="adonisControls:SplitButton" BasedOn="{StaticResource {x:Type adonisControls:SplitButton}}">
                <Setter Property="Padding" Value="12,7" />
                <Setter Property="MinHeight" Value="34" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Background" Value="#99213357" />
                <Setter Property="BorderBrush" Value="#664A90E2" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="adonisExtensions:CornerRadiusExtension.CornerRadius" Value="10" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UseDarkTheme, Source={x:Static settings:UserSettings.Default}}" Value="False">
                        <Setter Property="Foreground" Value="#FF1F2937" />
                        <Setter Property="Background" Value="#FFEAF2FF" />
                        <Setter Property="BorderBrush" Value="#335C8DC4" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="UpdateJP5Separator" TargetType="Separator" BasedOn="{StaticResource CustomSeparator}">
                <Setter Property="Margin" Value="0,2,0,10" />
                <Setter Property="Background" Value="#6A5EA3EC" />
                <Setter Property="Foreground" Value="#DCEBFF" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UseDarkTheme, Source={x:Static settings:UserSettings.Default}}" Value="False">
                        <Setter Property="Background" Value="#5B8FCB" />
                        <Setter Property="Foreground" Value="#33415E" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="UpdateJP5GroupBox" TargetType="GroupBox" BasedOn="{StaticResource {x:Type GroupBox}}">
                <Setter Property="Margin" Value="0,0,0,6" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="Background" Value="#1A1E2840" />
                <Setter Property="BorderBrush" Value="#2A4A6A90" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Foreground" Value="#9EC8FF" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="adonisExtensions:CornerRadiusExtension.CornerRadius" Value="10" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UseDarkTheme, Source={x:Static settings:UserSettings.Default}}" Value="False">
                        <Setter Property="Background" Value="#0F3A5580" />
                        <Setter Property="BorderBrush" Value="#335C8DC4" />
                        <Setter Property="Foreground" Value="#1A4A7A" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </adonisControls:AdonisWindow.Resources>
    <Grid>
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Background" Value="{StaticResource UpdateJP5WindowBackgroundDark}" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UseDarkTheme, Source={x:Static settings:UserSettings.Default}}" Value="False">
                        <Setter Property="Background" Value="{StaticResource UpdateJP5WindowBackgroundLight}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="12,12,12,8" Style="{StaticResource UpdateJP5Card}">
            <TextBlock TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"
                       FontSize="14" FontWeight="SemiBold"
                       Text="{DynamicResource Update_Description}"/>
        </Border>

        <Border Grid.Row="1" Margin="12,0,12,8" Style="{StaticResource UpdateJP5Card}">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
            <!-- <Grid.Style> -->
            <!--     <Style TargetType="Grid"> -->
            <!--         <Setter Property="Visibility" Value="Visible" /> -->
            <!--         <Style.Triggers> -->
            <!--             <DataTrigger Binding="{Binding Title, RelativeSource={RelativeSource AncestorType=adonisControls:AdonisWindow}}" Value="Releases"> -->
            <!--                 <Setter Property="Visibility" Value="Collapsed" /> -->
            <!--             </DataTrigger> -->
            <!--         </Style.Triggers> -->
            <!--     </Style> -->
            <!-- </Grid.Style> -->

            <Button Grid.Column="0" Style="{StaticResource UpdateJP5PrimaryButton}"
                    VerticalAlignment="Top" Height="{Binding ActualHeight, ElementName=RemindButton}"
                    Click="OnDownloadLatest" Content="{DynamicResource Update_DownloadLatest}" />

            <StackPanel Grid.Column="2">
                <adonisControls:SplitButton x:Name="RemindButton" Style="{StaticResource UpdateJP5SecondaryButton}" Content="{DynamicResource Update_RemindMe}" Command="{Binding RemindMeCommand}">
                    <adonisControls:SplitButton.SplitMenu>
                        <ContextMenu>
                            <MenuItem Header="{DynamicResource Update_Remind_3Days}" Command="{Binding RemindMeCommand}" CommandParameter="Days" />
                            <MenuItem Header="{DynamicResource Update_Remind_1Week}" Command="{Binding RemindMeCommand}" CommandParameter="Week" />
                            <MenuItem Header="{DynamicResource Update_Remind_1Month}" Command="{Binding RemindMeCommand}" CommandParameter="Month" />
                            <MenuItem Header="{DynamicResource Update_Remind_Never}" Command="{Binding RemindMeCommand}" CommandParameter="Never" />
                        </ContextMenu>
                    </adonisControls:SplitButton.SplitMenu>
                </adonisControls:SplitButton>
                <TextBlock VerticalAlignment="Bottom" HorizontalAlignment="Right" FontSize="10" Margin="0 2.5 0 0">
                    <Run Text="{DynamicResource Update_NextRefresh}"/>
                    <Run Text="{Binding NextUpdateCheck, Source={x:Static settings:UserSettings.Default}, StringFormat=': {0:MMM d, yyyy}'}"/>
                </TextBlock>
            </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Margin="12,0,12,12" Style="{StaticResource UpdateJP5Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Separator Grid.Row="0" Style="{StaticResource UpdateJP5Separator}" Tag="{DynamicResource Update_History}" />
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding CommitsView}">
                        <ItemsControl.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="GroupItem">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="GroupItem">
                                                    <GroupBox Style="{StaticResource UpdateJP5GroupBox}"
                                                              Header="{Binding Name}"
                                                              HeaderStringFormat="{DynamicResource Update_CommitsOn}">
                                                        <ItemsPresenter />
                                                    </GroupBox>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                                <GroupStyle.Panel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical" />
                                    </ItemsPanelTemplate>
                                </GroupStyle.Panel>
                            </GroupStyle>
                        </ItemsControl.GroupStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <controls:CommitControl Margin="0 0 0 1" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</adonisControls:AdonisWindow>
